body {
  background-color: #000000;
}

.countdown {
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 4rem;
  color: #ff0000;
}

.countdown-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin: 0 2rem;
}

.countdown-label {
  font-size: 1.5rem;
  font-weight: bold;
  text-transform: uppercase;
  margin-top: 0.5rem;
}
